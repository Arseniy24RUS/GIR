[pytest]
pythonpath = .
testpaths = tests
# The ddtrace auto-plugin can keep non-daemon telemetry threads alive after the
# suite has completed in isolated/offline delivery environments.  It is not
# part of the project QA contract, so disable it for deterministic exits.
addopts = -p no:ddtrace
