# Scientific-v5 evidence directory

Evidence from an earlier build is deliberately removed during patch application. Run the complete Playwright matrix, inspect visual diffs, and then execute `python scripts/record_playwright_v5_evidence.py`.
